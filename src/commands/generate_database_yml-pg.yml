description: Generate database.yml for PostgreSQL
steps:
  - run:
      name: Generate database.yml
      command: |
        cat \<<'EOF' > config/database.yml
        test:
          adapter: postgresql
          database: circle_test
          host: 127.0.0.1
          username: postgres
          password: postgres
        EOF
