description: Generate database.yml for MySQL
steps:
  - run:
      name: Generate database.yml
      command: |
        cat \<<'EOF' > config/database.yml
        test:
          adapter: mysql2
          database: circle_test
          host: 127.0.0.1
          username: root
          password:
          encoding: utf8mb4
        EOF
