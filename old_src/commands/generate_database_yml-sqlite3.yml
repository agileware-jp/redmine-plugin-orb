description: Generate database.yml for SQLite3
steps:
  - run:
      name: Generate database.yml
      command: |
        cat \<<'EOF' > config/database.yml
        test:
          adapter: sqlite3
          database: db/test.sqlite3
        EOF
